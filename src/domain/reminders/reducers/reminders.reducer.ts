import { IAction } from "../../../lib"

const initialState = {}

export function remindersReducer(state = initialState, action: IAction) {
  switch (action.type) {
    default:
      return state
  }
}
